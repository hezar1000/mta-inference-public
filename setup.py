import setuptools

setuptools.setup(
    name="mta_inference",
    description="Bayesian inference for peer grading",
    version=0.1,
    author="Hedayat Zarkoob",
    author_email="hzarkoob@cs.ubc.ca",
    packages=setuptools.find_packages(),
    python_requires='>=3.6',
)